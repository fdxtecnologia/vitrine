module(...,package.seeall);

function new()
    local login = {};
    
    function login:buildForm(parentGroup)
        
    end
    
    function login:authenticate(user, password)
        
    end
end
